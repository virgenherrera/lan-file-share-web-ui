export const enum SharedFolderRoute {
  sharedFolder = '/shared-folder',
  fileStream = '/shared-folder/file',
  zipFile = '/shared-folder/zip',
}

export const enum UploadRoute {
  mimeTypes = '/upload/mime-types',
  file = '/upload/file',
  files = '/upload/files',
}
